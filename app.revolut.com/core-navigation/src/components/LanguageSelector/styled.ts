import styled from 'styled-components'
import { Flex } from '@revolut/ui-kit'

import { themeColor } from '@revolut/rwa-core-styles'

export const StyledLabel = styled(Flex)`
  color: ${themeColor('languagePickerLabelLight')};
  align-items: center;
`
