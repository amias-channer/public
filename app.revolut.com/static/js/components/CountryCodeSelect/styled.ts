import styled from 'styled-components'
import { theme } from 'styled-tools'
import { InputSelect } from '@revolut/ui-kit'

export const StyledInputSelect = styled(InputSelect)`
  width: ${theme('sizes.px136')};
  max-width: 100%;
`
