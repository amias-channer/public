import * as React from 'react'
import { Text } from '@revolut/ui-kit'

export const TransactionsListFilterText = (props: React.ComponentProps<typeof Text>) => (
  <Text fontWeight="bolder" {...props} />
)
